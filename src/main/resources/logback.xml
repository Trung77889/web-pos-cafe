<configuration>
    <property name="LOG_DIR" value="D:/Java Servlet/zero-star-cafe/logs"/>

    <!-- Appender: where the log is written to (console, file, database...) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Log line format -->
            <pattern>%highlight(%d{HH:mm:ss} %-5level) %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>[%d{yyyy-MM-dd} %d{HH:mm:ss}] %-5level %logger{36} %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger (default log for entry application) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
